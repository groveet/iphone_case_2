= simple_form_for @item do |f|
  = f.collection_select :category_id, Category.all, :id, :name, {promt: 'Выбурите категорию товара'}

  = f.input :title
  = f.input :description
  = f.input :price
  = f.input :image, as: :file
  = f.button :submit, class: "btn btn-primary"